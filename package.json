{
  "name": "portfolio",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.18.11",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "build": "npx esbuild Typescript/mainTS.ts --bundle --outfile=Typescript/mainTS.bundle.js --platform=browser --format=esm",
    "watch": "npx esbuild Typescript/mainTS.ts --bundle --outfile=Typescript/mainTS.bundle.js --platform=browser --format=esm --watch"
  }
}
