{
  "name": "portfolio",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.18.11",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "build": "npx esbuild mainTS.ts --bundle --outfile=mainTS.bundle.js --platform=browser --format=esm",
    "watch": "npx esbuild mainTS.ts --bundle --outfile=mainTS.bundle.js --platform=browser --format=esm --watch"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "node-fetch": "^2.7.0"
  }
}
